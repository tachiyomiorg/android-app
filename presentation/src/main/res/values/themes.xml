<?xml version="1.0" encoding="utf-8"?>
<resources>

  <!-- Base application theme. These colors don't affect the Composable functions,
   just the color of the Status Bar. -->

  <style name="Theme.App" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="Theme.Splash" parent="Theme.App">
    <item name="colorBackgroundSplash">@color/splashBackgroundLight</item>

    <item name="android:windowBackground">@drawable/splash_background</item>
    <item name="android:statusBarColor">?attr/colorBackgroundSplash</item>
    <item name="android:navigationBarColor">?attr/colorBackgroundSplash</item>
  </style>

</resources>
